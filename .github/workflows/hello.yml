name: Hello World

on: push

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello World!"

      - name: Print date
        run: date

      - name: List files
        run: ls -l
